namespace Template.Common.Enums
{
    // Was an empty stub class inside Resource.cs. Converted to an enum since
    // it's used as a fixed classification, not an entity with its own data.
    // Matches the Documentation Requirements section of the IMTS spec:
    // Innovation Strategy, Innovation Policy and Guidelines, and other key documents.
    public enum ResourceCategory
    {
        InnovationStrategy,
        PolicyAndGuidelines,
        Template,
        TrainingMaterial,
        Other
    }
}
